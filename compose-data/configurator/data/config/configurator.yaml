server:
  log:
    level: debug
  domain: localhost
  name: demo-eventnative
  auth: 'a123'

storage:
  redis:
    host: redis
    port: 6379

jitsu:
  base_url: "http://server:8001"
  admin_token: 'a123'

auth:
  redis:
    host: redis
    port: 6379
    access_secret: access
    refresh_secret: refresh