server:
  log:
    level: debug
  domain: localhost
  name: demo-eventnative
  auth: 'a123'

storage:
  redis:
    host: redis
    port: 6379

eventnative:
  base_url: "http://server:8001/api/v1"
  admin_token: 'a123'


statistics:
  redis:
    host: redis
    port: 6379

auth:
  redis:
    host: redis
    port: 6379
    access_secret: access
    refresh_secret: refresh