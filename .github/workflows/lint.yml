name: 🧪Lint and Test
on:
  push:
  pull_request:
  workflow_dispatch:

env:
  # The jitsu-builder container has Playwright browsers binaries preinstalled on this path:
  PLAYWRIGHT_BROWSERS_PATH: /root/.cache/ms-playwright
  BETA_BRANCH: newjitsu
  STABLE_BRANCH: stable-services

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-format:
    name: ✨ Lint & Format
    timeout-minutes: 20
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 🚀 Setup Turbo cache
        uses: actions/cache@v4
        with:
          path: |
            .turbo
            **/node_modules/.cache
          key: ${{ runner.os }}-turbo-lint-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-turbo-lint-

      - name: 🐳 Pull and start builder container
        run: |
          docker pull ghcr.io/jitsucom/jitsu-builder:latest
          docker run -d --name builder -v ${{ github.workspace }}:/workspace -w /workspace -e CI=true ghcr.io/jitsucom/jitsu-builder:latest tail -f /dev/null

      - name: 📦 Fetch dependencies from store
        run: |
          docker exec builder sh -c 'echo "pnpm store path: $(pnpm store path)"'
          docker exec builder pnpm fetch

      - name: 📦 Install dependencies
        run: docker exec builder pnpm install --frozen-lockfile --offline

      - name: ✨ Check code format
        run: docker exec builder pnpm format:check:all

      - name: 🔧 Run codegen
        run: docker exec builder pnpm codegen

      - name: 🧪️ Run Typecheck
        run: docker exec builder pnpm typecheck:no-turbo

      - name: 🧪 Run linter
        run: docker exec builder pnpm lint

      - name: 🧹 Cleanup container
        if: always()
        run: docker stop builder && docker rm builder

  build-and-test:
    name: 🏗️ Run Test
    timeout-minutes: 40
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 🚀 Setup Turbo cache
        uses: actions/cache@v4
        with:
          path: |
            .turbo
            **/node_modules/.cache
          key: ${{ runner.os }}-turbo-build-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-turbo-build-

      - name: 🐳 Pull builder image
        run: |
          docker pull ghcr.io/jitsucom/jitsu-builder:latest

      - name: 🐳 Start builder container
        run: |
          docker run -d --name builder -v ${{ github.workspace }}:/workspace -w /workspace -e CI=true ghcr.io/jitsucom/jitsu-builder:latest tail -f /dev/null

      - name: 📦 Fetch dependencies from store
        run: |
          docker exec builder sh -c 'echo "pnpm store path: $(pnpm store path)"'
          docker exec builder pnpm fetch

      - name: 📦 Install dependencies
        run: docker exec builder pnpm install --frozen-lockfile --offline

      - name: 🧪 Run tests
        run: docker exec builder pnpm test:no-turbo

      - name: 🧹 Cleanup container
        if: always()
        run: docker stop builder && docker rm builder

  playwright:
    name: 🎭 E2E Tests
    timeout-minutes: 30
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 🔍 Check changed files
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            playwright:
              - 'libs/jitsu-js/**'
              - '**/playwright/**'
              - '**/*.spec.ts'
              - '**/playwright.config.ts'

      - name: 🚀 Setup Turbo cache
        if: steps.changes.outputs.playwright == 'true'
        uses: actions/cache@v4
        with:
          path: |
            .turbo
            **/node_modules/.cache
          key: ${{ runner.os }}-turbo-playwright-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-turbo-playwright-

      - name: 🐳 Pull and start builder container
        if: steps.changes.outputs.playwright == 'true'
        run: |
          docker pull ghcr.io/jitsucom/jitsu-builder:latest
          docker run -d --name builder -v ${{ github.workspace }}:/workspace -w /workspace -e PLAYWRIGHT_BROWSERS_PATH=/root/.cache/ms-playwright -e CI=true ghcr.io/jitsucom/jitsu-builder:latest tail -f /dev/null

      - name: 📦 Install dependencies
        if: steps.changes.outputs.playwright == 'true'
        run: docker exec builder pnpm install --frozen-lockfile --prefer-offline

      - name: 🏗️ Build (for Playwright)
        if: steps.changes.outputs.playwright == 'true'
        run: docker exec builder sh -c 'pnpm turbo run build --filter="!@jitsu-internal/console" --filter="!@jitsu-ee/ee-api"'

      - name: 🎭 Run Playwright tests
        if: steps.changes.outputs.playwright == 'true'
        run: docker exec builder pnpm test:playwright

      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v4
        if: always() && steps.changes.outputs.playwright == 'true'
        with:
          path: libs/jitsu-js/playwright/artifacts
          name: artifacts/@jitsu/js
          retention-days: 30

      - name: 🧹 Cleanup container
        if: always() && steps.changes.outputs.playwright == 'true'
        run: docker stop builder && docker rm builder

  notify:
    name: 📢 Notify
    needs: [lint-and-format, build-and-test, playwright]
    if: always() && github.ref == 'refs/heads/newjitsu'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📝 Get short SHA
        id: short-sha
        run: echo "sha=$(git rev-parse --short=7 HEAD)" >> $GITHUB_OUTPUT

      - name: 🔍 Check job statuses
        id: status
        run: |
          if [[ "${{ needs.lint-and-format.result }}" == "success" && "${{ needs.build-and-test.result }}" == "success" && ("${{ needs.playwright.result }}" == "success" || "${{ needs.playwright.result }}" == "skipped") ]]; then
            echo "success=true" >> $GITHUB_OUTPUT
            echo "color=#36a64f" >> $GITHUB_OUTPUT
            echo "header=✅ Lint & Test on branch \`${{ github.ref_name }}\` SUCCEEDED" >> $GITHUB_OUTPUT
          else
            echo "success=false" >> $GITHUB_OUTPUT
            echo "color=#ff0000" >> $GITHUB_OUTPUT
            echo "header=❌ Lint & Test on branch \`${{ github.ref_name }}\` FAILED" >> $GITHUB_OUTPUT
          fi

      - name: 💬 Send notification to Slack
        uses: ./.github/actions/slack-notify
        with:
          slack_webhook_url: ${{ secrets.CI_SLACK_WEBHOOK }}
          color: ${{ steps.status.outputs.color }}
          header: ${{ steps.status.outputs.header }}
          blocks: |
            - title: Commit
              value: ${{ steps.short-sha.outputs.sha }}
              url: ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}
            - title: Commit message
              value: ${{ toJSON(github.event.head_commit.message) }}
              is_code: true
            - title: Workflow run
              value: ${{ steps.status.outputs.success == 'true' && 'View workflow run' || 'View failed run' }}
              url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
            - title: Triggered by
              value: ${{ github.actor }}

  deploy:
    name: 🐳 Build Docker Images
    needs: [lint-and-format, build-and-test, playwright]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/newjitsu' || github.ref == 'refs/heads/stable-services')
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    uses: ./.github/workflows/services-build.yaml
    secrets: inherit
    # Note: skip_install doesn't work as packages are installed inside docker
