import { UpdateConfigStaticFieldsValues } from './SourceEditorFormConfiguration';

type Props = {
  onChange: UpdateConfigStaticFieldsValues;
};

export const SourceEditorFormConfigurationStaticFields: React.FC<Props> =
  () => {
    // const form = useForm()

    // useEffect(() => { onChange(form.getFieldsValues) })
    return (
      // <Form form={form} />

      // Render predefined fields that are common for all sources

      null
    );
  };
